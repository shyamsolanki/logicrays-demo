// This function will perform the operation of LogIn

export function Login(props){

    const disp_login=
    <div>
        <button onClick={props.clickInfo}> LOGIN </button>

    </div>
    return disp_login;
}
